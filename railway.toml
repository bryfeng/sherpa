[build]
builder = "NIXPACKS"

[variables]
PYTHON_VERSION = "3.11"

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "pip install ."
]

[start]
# adjust module path if app is not in main.py
cmd = "uvicorn agentic_wallet_py.main:app --host 0.0.0.0 --port $PORT"
